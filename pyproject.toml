[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "my-deepracer-project"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
  "stable-baselines3",
]

[tool.setuptools]
py-modules = ["train", "reward"]
